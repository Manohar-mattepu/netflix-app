apiVersion: v1
kind: Service
metadata:
  name: netflix-app-service
spec:
  type: NodePort
  selector:
    app: netflix-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
